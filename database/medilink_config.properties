# MediLink 2.0 PostgreSQL Database Configuration
db.url=jdbc:postgresql://localhost:5433/medilink_db
db.user=postgres
db.password=${DB_PASSWORD}

# MediLink 2.0 Gmail SMTP Configuration (for sending real emails to recipients)
# Enter your Gmail address and 16-character Google App Password below:
mail.sender=
mail.password=
